#!/usr/bin/env bash

INPUT_URL="${INPUT_URL:-http://192.168.200.219/input}"
START_RELAY_URL="${START_RELAY_URL:-http://192.168.200.219/relay/0}"

DATASET_DIR="${DATASET_DIR:-../dataset}"

PHOTO_TRIGGER_PIN="${PHOTO_TRIGGER_PIN:-33}"
CAMERA_PATH="${CAMERA_PATH:-/dev/video2}"
MAX_CAPTURES="${MAX_CAPTURES:-20}"

BUILD_DIR="${BUILD_DIR:-build}"
EXECUTABLE_NAME="${EXECUTABLE_NAME:-ur5_capture}"